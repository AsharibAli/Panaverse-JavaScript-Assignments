var users = ['Ali','Ahmed','Ibrahim','Yousaf','Bilal'];
 while (users.length > 0) {
    users.pop();
}
console.log(users.length);
if ( users.length == 0) {
    console.log("We need to find some users!");
};
